class Canvas {
    width;
    height;
    
    /**
     * @private
     * @type {HTMLCanvasElement}
     */
    element;
    /**
     * @private
     * @type {any[]}
     */
    children;

    constructor(id, width, height)
    {

    }
}
